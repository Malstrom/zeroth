# Tensho Framework — Overview

## Purpose
GTM (Go-To-Market) tracker. Centralises strategy, decisions, and open tasks across all products in a single repo.

## Status
`work in progress` — rules to be defined.

## Known Principles
- One repo for all products (not one per product)
- Decisions are logged and immutable
- AI reads context from `.agent.yml` at session start
