# tensho framework — overview

framework: tensho
purpose: GTM (Go-To-Market) tracker — centralises strategy, decisions, and open tasks across all products in a single repo
status: work-in-progress

known_principles:
  - one repo for all products (not one per product)
  - decisions are logged and immutable
  - AI reads context from .agent.yml at session start
