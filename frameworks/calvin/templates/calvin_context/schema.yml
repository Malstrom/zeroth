# .calvin/schema.yml — auto-generated, do not edit manually
# Source: db/schema.rb + app/models/*.rb
# Regenerated by calvin.rb at startup and when db/schema.rb changes
#
# Format:
# ModelName:
#   table: table_name
#   columns: [id, col1, col2, ...]
#   belongs_to: [OtherModel]
#   has_many: [OtherModel]
#   enums: {field: [val1, val2]}
#
# Example:
# User:
#   table: users
#   columns: [id, email, name, role, created_at, updated_at]
#   belongs_to: []
#   has_many: [orders, sessions]
#   enums: {role: [admin, member, guest]}
#
# At execution time, only models relevant to the current issue are injected.
# Relevance: model names (PascalCase) found in issue title + async-ready comment.
