decisions: []

# Append-only — never modify or remove existing entries.
#
# Add a new entry after each PR merge that introduces an architectural decision.
# Source: Calvin notes in PR body → confirmed by Igor via review_pr scenario.
#
# Format:
# - date: "YYYY-MM-DD"
#   decision: "Use dry-validation for all input contracts"
#   rationale: "Consistent validation layer, integrates with dry-rb ecosystem already in use"
#   source: "PR #42"
#
# - date: "YYYY-MM-DD"
#   decision: "Jobs must be idempotent — always safe to retry"
#   rationale: "Sidekiq retries on failure; non-idempotent jobs cause data corruption"
#   source: "PR #67"
