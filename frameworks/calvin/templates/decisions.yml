# .calvin/decisions.yml
# Qwen: these are hard constraints — never introduce rejected patterns.
# Append-only. Never delete entries.
# Format: one constraint per line, imperative.

decisions: []

# Example entries (remove this comment after first real decision):
# decisions:
#   - "serializer: blueprinter, not jbuilder"
#   - "no after_create callbacks — use explicit service calls"
#   - "AuditLog: no updated_at — append-only table"
