# .gakusei.yml — student identity
# Managed by: dojo framework
# This file contains ONLY non-derivable student metadata.
# Topic state is derived at runtime from: goal issues + kata files + nikki filenames.
# DO NOT add topics.*, goals.*, or last_session.* — they are intentionally absent.

student_name: ""
language: ""                    # chat language, e.g. italian, english
issue_language: "english"       # language for issue/PR bodies and file content
