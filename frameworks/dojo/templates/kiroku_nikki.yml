# kiroku/nikki template
# Produced by: study_open, topic_shinsa, goal_shinsa
# Filename pattern: YYYY-MM-DD_{topic}_{type}.yml
#   type: study | shinsa | goal_shinsa

date: "{date}"
topic: "{topic}"
type: "{type}"                    # study | shinsa | goal_shinsa
session_number: {session_number}  # incremental per topic

summary: |
  {summary}

weak_spots:
  - {weak_spot}

randori_score:
  correct: {correct}
  total: {total}
  pct: {pct}

notes: |
  {notes}
