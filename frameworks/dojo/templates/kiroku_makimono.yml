# kiroku/makimono template
# Produced by: goal_shinsa on pass only
# Filename pattern: YYYY-MM-DD_{goal_slug}_passed.yml
# Immutable — write-once, never updated after creation.

date: "{date}"
goal: "{goal_slug}"
result: passed
score: {score}

nikki_ref: "kiroku/nikki/YYYY-MM-DD_{goal_slug}_goal_shinsa.yml"

topics_covered:
  - {topic}

notes: |
  {notes}
