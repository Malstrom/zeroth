# frameworks/sudo-hire-me/structure.yml
# Directory structure for sudo-hire-me instance repos.
# Used by zeroth spawn scenario to scaffold a new instance.
# Status: STUB — verify against actual Malstrom/sudo-hire-me layout

framework: sudo-hire-me

# TODO: confirm and complete this map against the real repo structure
directories:
  hunt/:
    role: active job search
    subdirs:
      pipeline/: per-company logs
      cv/: CVs by role and language
      letters/sent/: sent cover letters

  forge/:
    role: project portfolio work

  idea/:
    role: raw ideas and notes

# TODO: define required files at root level
root_files:
  - igor.md           # full profile — TODO: generalize to profile.md for non-Igor instances
  - hunt/tracker.md   # active pipeline dashboard
