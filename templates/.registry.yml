# .registry.yml
# AI-only. Connection map for cross-repo sync.
# Read by post_action_hook after every state_change. Never read proactively.
# Spec: github.com/Malstrom/zeroth

framework: {{FRAMEWORK_NAME}}
version: "1.0.0"

connections: []
# connection template:
# - repo: owner/other-repo
#   role: description of what this connection is used for
#   sync_trigger:
#     - state-change-event-name
#   access: read | read-write
