# {framework-name}

> {one-line fundamental question this framework answers}

---

## 🧠 Why {framework-name} exists

<!--
  This is the manifesto. Be opinionated. Cite real research.

  Structure for every research point:

    ***Problem title — what goes wrong without this framework.***

    One plain-language sentence. No jargon. [Author, Year](link).

  Example (from dojo):

    ***You remember what you retrieve, not what you read.***

    Recalling information actively beats re-reading it every time — the act of retrieval
    itself is what builds durable memory. [Roediger & Karpicke, 2006](https://doi.org/10.1177/1745691612443552).

  Rules:
  - Minimum 3 research points.
  - Each point = bold+italic title on its own line + 1 plain-language sentence below + 1 live link.
  - Blank line between every point — no bullet list.
  - End with 2 closing paragraphs:
      1. The AI trap — why AI makes this problem worse if used naively.
      2. The goal — what this framework actually achieves (outcome, not feature list).
-->

***{Problem title — what goes wrong without this framework.}***

{One plain-language sentence. No jargon.} [{Author, Year}]({link}).

***{Problem title — what goes wrong without this framework.}***

{One plain-language sentence. No jargon.} [{Author, Year}]({link}).

***{Problem title — what goes wrong without this framework.}***

{One plain-language sentence. No jargon.} [{Author, Year}]({link}).

{The AI trap: why using AI naively makes this problem worse, not better.}

{The goal: one or two sentences on what this framework actually achieves — outcome, not features.}

---

## ⚙️ How it works

<!--
  5–8 bullets. Each bullet = one concrete thing that happens in a session or over time.
  Cover the full loop: input → session → output → persistence.
  No philosophy here — that belongs in Why.
  No file names or spec details — those belong in overview.yml and structure.yml.

  Example (from dojo):
    - The AI reads your knowledge state at the start of every session —
      what you know, what you have studied, how you performed.
    - The session works on the delta: what you do not know yet, or what you know weakly.
    - At the end of every session, the AI writes a structured trace to the repo:
      topics covered, results, updated knowledge state.
-->

- ...
- ...
- ...

---

## 🚀 How to start

Create a new {framework-name} instance using the `spawn` scenario from your zeroth Perplexity Space.
