name: giskard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  zeroth:
    name: zeroth
    runs-on: ubuntu-latest

    steps:
      - name: Checkout zeroth
        uses: actions/checkout@v4

      - name: zeroth
        uses: Malstrom/giskard@main
        with:
          repo: .
          mode: zeroth
          github-token: ${{ secrets.GITHUB_TOKEN }}
